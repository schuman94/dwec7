const boton = document.getElementById('cambiaContenido');

function cambiarContenido() {
    const xhr = new XMLHttpRequest();

    xhr.open('GET', 'holamundoS.txt');

    xhr.onreadystatechange = function () {
        
    }
}